// #1 unary operator -  typeof - has only just type type of operand
//how many operands do they have

let text = "some text";
// console.log(typeof text); // operand


// #2 binary operator - assignment - has to operands : first : declaration and the other: assignment
let number = 3;
let number2 = 2 + 5;

// ternary operator
// condition ? (runs if true) : (runs if false)

const value = 1 < 0;

value ? console.log("value is true") : console.log("value is false");
// if the value is thrue, if its falce then do something else

// if (value) {
//   console.log('value is true');
// } else {
//   console.log('value is false');
// }
